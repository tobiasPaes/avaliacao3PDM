name: terceira_prova
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  floor: ^1.4.2
  connectivity_plus: ^5.0.2
  http: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"
  flutter_lints: ^2.0.0
  floor_generator: ^1.4.2
  build_runner: ^2.1.2

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/poke.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true
